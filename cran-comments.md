## R CMD check results

0 errors | 0 warnings | 0 notes

* This is a new submission.

* All examples run in < 5 seconds.

* There are no calls to `globalVariables()`.

* The package does not use any compiled code (NeedsCompilation: no).

* The package has been tested on:
  - macOS (R release)
  - Windows (R release)
  - Ubuntu (R devel, release, oldrel-1)

* Two datasets are referenced in examples but no data files are required;
  all examples use simulated data generated by built-in functions.

## Downstream dependencies

None (new package).
