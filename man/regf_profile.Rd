% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/residual_entropy.R
\name{regf_profile}
\alias{regf_profile}
\title{REGF Profile Over Alpha}
\usage{
regf_profile(
  dens_fn,
  surv_fn = NULL,
  alpha_grid = seq(0.1, 3, by = 0.2),
  t = 0,
  upper = 100,
  plot = FALSE
)
}
\arguments{
\item{dens_fn}{Density function.}

\item{surv_fn}{Survival function; computed internally if \code{NULL}.}

\item{alpha_grid}{Numeric vector of \eqn{\alpha} values (default 0.1 to 3).}

\item{t}{Truncation time.}

\item{upper}{Integration upper bound.}

\item{plot}{Logical; if \code{TRUE} plots the profile (default \code{FALSE}).}
}
\value{
A data frame with columns \code{alpha} and \code{REGF}.
}
\description{
Evaluates \code{\link{residual_info_gen}} over a grid of \eqn{\alpha} values
and optionally plots the result.  Useful for studying how the information
content varies with the Renyi-type parameter.
}
\examples{
f  <- function(x) dexp(x, 1)
Fb <- function(x) pexp(x, 1, lower.tail = FALSE)
regf_profile(f, Fb, t = 0.5, plot = FALSE)

}
